services:
  - type: web
    name: xhs-assistant-api
    runtime: node
    buildCommand: npm install
    startCommand: node deploy-render/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
