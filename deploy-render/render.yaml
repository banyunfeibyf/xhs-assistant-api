services:
  - type: web
    name: xhs-assistant-api
    runtime: node
    rootDir: deploy-render
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
